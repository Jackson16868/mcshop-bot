# === Flask / App ===
# 伺服器埠號（預設 5001）
PORT=5001

# SQLAlchemy 連線字串（Postgres 範例）
# postgresql+psycopg2://USER:PASSWORD@HOST:5432/DBNAME
DATABASE_URL=postgresql+psycopg2://postgres:password@localhost:5432/mcshop

# 管理後台帳密（/admin）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# === LINE Bot ===
LINE_CHANNEL_SECRET=YOUR_LINE_CHANNEL_SECRET
LINE_CHANNEL_ACCESS_TOKEN=YOUR_LONG_LIVED_CHANNEL_ACCESS_TOKEN

# 若要開啟 Flex（未來要用時再打開）
# ENABLE_FLEX=1

# === Webhook（說明用，不會被 app 直接讀） ===
# 在設 LINE Webhook 時會用到（例如 ngrok domain）
# WEBHOOK_URL=https://<your-ngrok-subdomain>.ngrok.io/callback
